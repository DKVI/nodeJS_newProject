<div class="container">
  <h1>Khóa học của tôi</h1>
  {{#if countDeleted}}
    <a href="/me/trash/courses">🗑️ Thùng rác {{countDeleted}}</a>
  {{/if}}
  <p>{{countUnDeleted}}</p>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">STT</th>
        <th scope="col">Tên khóa học</th>
        <th scope="col">Mô tả</th>
        <th scope="col">Level</th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody>
      {{#each courses}}

        <tr>
          <th scope="row"><input type="checkbox" id="checkbox" name="{{this._id}}" />
            {{sum @index 1}}</th>
          <td>{{this.name}}</td>
          <td>{{this.description}}</td>
          <td>{{this.level}}</td>
          <td>
            <a href="/courses/{{this._id}}/edit" class="btn btn-link">Sửa</a>
            <a
              class="btn btn-link delete-course-btn"
              data-toggle="modal"
              href=""
              data-target="#delete-course-modal"
              data-course-id="{{this._id}}"
            >Xóa</a>
          </td>
        </tr>
        <!-- Modal -->

      {{else}}
        <tr class="text-center">
          <td colspan="5">Không có khóa học nào!
            <a href="/courses/create">Thêm khóa học mới</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
  <table class="table">
    <th scope="row">
      Check All
      <input type="checkbox" name="checkAll" id="" />
    </th>
    <td></td>
    <td></td>
    <td></td>
    <td><form class="final-submit-form">
        <button type="submit" class="btn btn-primary final-remove-btn">Xóa</button>
      </form></td>
  </table>

</div>

<form>
  <div
    class="modal fade"
    id="delete-course-modal"
    tabindex="-1"
    role="dialog"
    aria-labelledby="deleteModalLabel"
    aria-hidden="true"
  >
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="deleteModalLabel">Delete Confirmation</h5>
          <button
            type="button"
            class="close"
            data-dismiss="modal"
            aria-label="Close"
          >
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          Are you sure you want to delete this selection?
        </div>
        <div class="modal-footer">
          <button
            type="button"
            class="btn btn-secondary"
            data-dismiss="modal"
          >Cancel</button>
          <button type="submit" class="btn btn-danger">Delete</button>
        </div>
      </div>
    </div>
  </div>
</form>

<script>
  const idList = [];
  const distinctValues = function (value, array) {
    array.forEach ((item, index) => {
      if (item === value) {
        array.splice(index, 1);
      }
      else return;
    })
  }
  const convertToReqBody = function (array) {
    let reqBody = {};
    array.forEach ((item, index) => {
      reqBody[index] = item;
    })
    return reqBody;
  }
  const deleteBtn = document.querySelectorAll('.delete-course-btn'); const
  formSubmit = document.querySelector('form'); deleteBtn.forEach(btn => {
  btn.addEventListener('click', function(e) { 
    const courseId = e.target.dataset.courseId; 
    formSubmit.action = `/courses/${courseId}?_method=PATCH`; 
    formSubmit.method = 'POST'; 
  }) 
  })
  formSubmit.addEventListener('submit', function(e) { e.preventDefault();
  formSubmit.submit(); }) 
  const checkAllBtn = document.querySelector('input[name="checkAll"]'); 
  const checkboxes = document.querySelectorAll('#checkbox'); 
  checkboxes.forEach ((item) => {
    item.addEventListener('change', function(e) {
        if (e.target.checked) {
        idList.push(e.target.name);
      } else {
        idList.pop(e.target.name);
      }
      console.log (idList);
    })
  })
  console.log (checkAllBtn);
  checkAllBtn.onchange = () => {

    if (checkAllBtn.checked) {
        checkboxes.forEach ((item) => {
            item.checked = true;
            distinctValues(item.name, idList); 
            idList.push(item.name);
        })
    } 
    else {
      checkboxes.forEach ((item) => {
            item.checked = false;
            distinctValues(item.name, idList); 
            idList.pop(item.name);
        })
    }
        console.log (idList);
  }
    const finalSubmitForm = document.querySelector(".final-submit-form");  
    const finalRemoveBtn = document.querySelector(".final-remove-btn");
    finalRemoveBtn.onclick = async () => {
      if (idList.length) {
        const reqBody = convertToReqBody(idList);
        await fetch (`/courses/multiple-delete`, {
          method: "DELETE",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify(reqBody),
        })
      }
    }
</script>